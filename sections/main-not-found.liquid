<style>
  {%- comment -%}
  Fills the full viewport height (not just whatever the icon/heading need), so the color-scheme
  background covers the page top to bottom regardless of content length. svh (not vh) so mobile
  browser toolbars don't leave a gap at the bottom when they're expanded.
  {%- endcomment -%}
  #shopify-section-{{ section.id }} > div {
    min-height: 100svh;
    display: flex;
    align-items: center;
  }

  #shopify-section-{{ section.id }} .container,
  #shopify-section-{{ section.id }} .empty-state {
    width: 100%;
  }

  #shopify-section-{{ section.id }} .empty-state {
    padding-block: 3rem;
    gap: 1.5rem;
  }

  {%- comment -%}
  Sizes the broken-screen illustration relative to viewport (like the old giant "404" number used to),
  capped so it never gets absurdly large on wide screens.
  {%- endcomment -%}
  #shopify-section-{{ section.id }} .not-found-icon {
    width: min(9rem, 26vw);
    height: auto;
    color: rgb(var(--text-color) / .4);
  }

  #shopify-section-{{ section.id }} .not-found-heading {
    color: #ffffff;
  }
</style>

<div class="color-scheme color-scheme--{{ section.settings.color_scheme.id }}{% if section.settings.floating_not_found %} section-floating section-floating--block-end{% endif %}"{% if section.settings.floating_not_found %} style="border-end-start-radius: {{ section.settings.not_found_border_radius }}px; border-end-end-radius: {{ section.settings.not_found_border_radius }}px; overflow: hidden;"{% endif %}>
  <div class="container">
    <div class="empty-state">
      {%- comment -%} Decorative "broken screen" illustration - no text content, so aria-hidden. {%- endcomment -%}
      <svg class="not-found-icon" aria-hidden="true" focusable="false" viewBox="0 0 160 140" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <rect x="20" y="15" width="120" height="80" rx="10"/>
        <path d="M75 95v15M55 110h50"/>
        <path d="M95 55 60 25M95 55 30 40M95 55 35 75M95 55 70 90M95 55 120 30M95 55 125 70M95 55 100 87"/>
        <path d="m89 49 9-7 4 12-11 3z"/>
        <path d="m24 108 8-6M132 100l7 7M118 108l5 9"/>
      </svg>

      {%- if section.settings.heading != blank -%}
        <h1 class="not-found-heading {{ section.settings.heading_size }}">{{ section.settings.heading }}</h1>
      {%- endif -%}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "t:sections.main_not_found.name",
  "class": "shopify-section--404",
  "tag": "section",
  "settings" : [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:global.colors.scheme",
      "default": "scheme-2"
    },
    {
      "type": "checkbox",
      "id": "floating_not_found",
      "label": "Floating 404",
      "info": "Adds margins around the section (bottom and sides only), using the store's global side margin.",
      "default": false
    },
    {
      "type": "range",
      "id": "not_found_border_radius",
      "min": 0,
      "max": 30,
      "step": 2,
      "unit": "px",
      "label": "Corner radius",
      "default": 0,
      "visible_if": "{{ section.settings.floating_not_found }}"
    },
    {
      "type": "header",
      "content": "t:global.text.text"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "t:global.text.heading",
      "default": "Page not found"
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "t:global.text.size",
      "options": [
        { "value": "display-xl", "label": "Display XL" },
        { "value": "display-l", "label": "Display L" },
        { "value": "display-m", "label": "Display M" },
        { "value": "h1", "label": "XX-Large" },
        { "value": "h2", "label": "X-Large" },
        { "value": "h3", "label": "Large" },
        { "value": "h4", "label": "Medium" },
        { "value": "h5", "label": "Small" },
        { "value": "h6", "label": "X-Small" },
        { "value": "h7", "label": "XX-Small" }
      ],
      "default": "h3"
    }
  ]
}
{% endschema %}
